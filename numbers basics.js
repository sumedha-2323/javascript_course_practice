// loosely typed - doesn't depends on the data type..any variable can be used to any of the data type

const number = 123;
let decimalNum = 2.556;
// decimalNum = "Sumedha";
console.log(number);
console.log(decimalNum);
let sum = number + decimalNum;
console.log(sum); // +, -, *, /, %  operations ca be performed
// In the console, numbers will be in blue color and strings will be in black color

// +=, -=, *=, /=, ++, -- operations can also be done
